namespace BookStudio.OpenCode;

/// <summary>Identifies the OpenCode external-adapter assembly for compatibility, session lifecycle, bounded SSE reconciliation, bounded cross-snapshot status history, deterministic reconnect and integration discovery.</summary>
public static class AssemblyMarker
{
}
