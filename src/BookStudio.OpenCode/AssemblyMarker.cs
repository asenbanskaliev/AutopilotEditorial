namespace BookStudio.OpenCode;

/// <summary>Identifies the OpenCode external-adapter assembly for compatibility, session lifecycle, bounded SSE event reconciliation, polling repair and integration discovery.</summary>
public static class AssemblyMarker
{
}
