namespace BookStudio.OpenCode;

/// <summary>Identifies the audited OpenCode external adapter for compatibility, session lifecycle, bounded SSE reconciliation, bounded cross-snapshot status history, deterministic reconnect and integration discovery.</summary>
public static class AssemblyMarker
{
}
