namespace BookStudio.OpenCode;

/// <summary>Identifies the fully audited OpenCode external adapter for compatibility, lifecycle, bounded SSE reconciliation, closed-trust-boundary agent-tool profile mapping and integration discovery.</summary>
public static class AssemblyMarker
{
}
