namespace BookStudio.OpenCode;

/// <summary>Identifies the audited OpenCode external adapter for compatibility, lifecycle, bounded SSE reconciliation, deterministic value-equal deny-by-default agent-tool profile mapping and integration discovery.</summary>
public static class AssemblyMarker
{
}
