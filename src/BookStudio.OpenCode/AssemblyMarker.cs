namespace BookStudio.OpenCode;

/// <summary>Identifies the audited OpenCode external adapter for compatibility, lifecycle, bounded SSE reconciliation, deterministic deny-by-default agent-tool policy mapping and integration discovery.</summary>
public static class AssemblyMarker
{
}
