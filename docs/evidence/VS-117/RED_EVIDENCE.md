# VS-117 Dual TDD RED evidence

## RED-I — implementation-facing

Expected contracts and behavior are intentionally absent at slice start:

- no typed proof request, exact VS-116 authority, checklist execution, finding, physical receipt, decision or state contracts;
- no deterministic checklist orchestration, annotation normalization or immutable proof evidence digest construction;
- no fail-closed approval over blocking proof findings or governed correction cycles;
- no replay-safe, concurrency-safe durable SQLite store, append-only history or Outbox for proof workflows.

## RED-E — external/governance-facing

The cumulative journey cannot yet prove that:

1. one exact approved VS-116 package authority produces deterministic digital-galley and physical-proof evidence;
2. checklist identity, version, rules, artifact input and output digests are explicit and reproducible;
3. trim, margins, bleed, pagination, typography, images, color, links, metadata, cover alignment and readability findings are normalized and traceable;
4. stale, missing, digest-mismatched, cross-workspace or non-approved inputs fail closed;
5. unresolved blocking findings prevent approval and corrections supersede rather than mutate frozen proof evidence;
6. physical-proof receipt and reviewer attestation are durable without implying external KDP acceptance;
7. restart and exact replay preserve one authoritative proof result and one deterministic Outbox effect;
8. transaction failure leaves no partial approved proof state.

No PASS claim is permitted until implementation, cumulative tests, GREEN_EVIDENCE, Auditoría M, Meta-Audit, RetroSpec, Plan Integrity, Governance Gates and .NET CI are complete and green on one final SHA.
