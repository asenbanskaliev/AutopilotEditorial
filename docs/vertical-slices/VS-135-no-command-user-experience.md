# VS-135 — No-Command User Experience

A persistent natural-language conversation collects idea, audience, format, chapter count and tone, validates answers conversationally, survives restart and launches the autonomous journey exactly once. User-facing replies never expose MCP names, tool identifiers, artifact IDs or shell commands.
