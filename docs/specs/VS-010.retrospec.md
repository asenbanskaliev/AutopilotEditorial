# VS-010 — RetroSpec

## Implemented behavior

- The repository pins .NET SDK `10.0.204` with patch-only roll-forward.
- `BookStudio.slnx` contains eight source projects and one architecture-fitness project.
- Central build policy targets `net10.0`, enables nullable and implicit usings, produces deterministic builds and treats warnings as errors.
- Central Package Management is enabled before external packages are introduced.
- Domain has no project dependencies.
- Application depends only on Domain.
- Infrastructure depends on Application and Domain.
- OpenCode depends only on Application.
- Autopilot depends on Application and Domain.
- MCP depends on Application and Infrastructure.
- Worker depends on Autopilot, Infrastructure and OpenCode.
- ControlCenter depends on Application and Infrastructure.
- Architecture fitness has no project dependencies and validates the repository files directly.
- ControlCenter exposes a minimal `/health` shell; MCP and Worker have compilable host entry points.
- `.NET CI` restores, builds, runs architecture fitness and publishes normalized evidence.

## Contracts

- SDK: `global.json`.
- Build policy: `Directory.Build.props`.
- Package policy: `Directory.Packages.props`.
- Solution membership: `BookStudio.slnx`.
- Reference graph: `docs/specs/VS-010.md` and architecture test implementations.
- CI contract: `.github/workflows/02-dotnet-ci.yml` and `dotnet.solution-baseline`.

## Approved deviations

The initial external GREEN named the self-hosted provider. `VS-010-CHANGE-001` approved GitHub-hosted for this lightweight baseline because the self-hosted job remained unclaimed. The technical commands and evidence contract did not change.

## Evidence

- RED Governance run: `30209623728`.
- RED Governance job: `89813554535`.
- GREEN Governance run: `30210048707`.
- GREEN Plan Integrity run: `30210048705`.
- GREEN .NET CI run: `30210048701`.
- GREEN .NET job: `89814620155`.
- Evidence artifact ID: `8634183922`.
- Evidence digest: `sha256:5455d2b1c35954e26a235f1b796a3ae21927225e32b3f09973ae832786faa97d`.

## Residual risks

- The composition hosts do not yet implement runtime behavior; their concrete responsibilities belong to later slices.
- External test frameworks have not been introduced; architecture fitness is an executable baseline and package-backed tests will be added with domain/application slices.
- Self-hosted runner availability remains to be certified in operations.

## Result

`RETROSPEC_SYNCED`
